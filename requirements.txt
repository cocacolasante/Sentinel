# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# LLM SDKs
anthropic==0.40.0
openai==1.57.0

# Memory
redis==5.2.1
psycopg2-binary==2.9.10
qdrant-client==1.12.1

# Slack
slack-bolt==1.21.1
aiohttp==3.11.11

# Phase 2 — Google APIs
google-auth==2.37.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
google-api-python-client==2.159.0

# Config & utilities
pydantic-settings==2.7.0
pydantic==2.10.3
httpx==0.28.1
python-dotenv==1.0.1
tenacity==9.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Observability
loguru==0.7.3
sentry-sdk[fastapi]==2.19.2

# Eval scheduler (kept as fallback; Celery Beat is primary)
apscheduler==3.10.4

# Celery task queue + Flower UI
celery[redis]==5.4.0
flower==2.0.1

# Prometheus metrics
prometheus-fastapi-instrumentator==7.0.0

# WhatsApp / Twilio (optional — leave TWILIO_* blank in .env to disable)
twilio==9.4.1

# Form parsing for webhooks (required for WhatsApp + any multipart endpoint)
python-multipart==0.0.20

# Knowledge Graph
neo4j>=5.0

# Cron schedule parsing (Reddit digest scheduler)
croniter>=1.3

# Infrastructure autonomy skills (Phase 4)
dnspython>=2.6
unidiff>=0.7
boto3>=1.34        # BackupVerifySkill — S3 backup checks (optional)
prometheus-client>=0.20

# Async Redis client (Phase 4+5)
# redis[asyncio] is already included via redis==5.2.1 above
